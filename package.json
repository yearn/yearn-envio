{
  "name": "envio-indexer",
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "clean": "tsc --clean",
    "build": "tsc --build",
    "allocation:fixture:check": "node scripts/allocation/verify-shared-debt-allocator.mjs",
    "allocation:test": "vitest run test/allocation",
    "check:config": "node scripts/check_envio_config_compatibility.mjs",
    "watch": "tsc --watch",
    "mocha": "vitest run",
    "codegen": "envio codegen",
    "dev": "envio dev",
    "migrate": "envio local db-migrate up",
    "start": "envio start",
    "start:envio": "envio start",
    "sync:vaults": "node scripts/update_kong_vaults.js",
    "test": "vitest run",
    "calc-fees": "tsc -p tsconfig.calc.json && node build/calc/calculate-depositor-fees.js",
    "jwt:generate": "node scripts/generate_hasura_jwt.js"
  },
  "devDependencies": {
    "@types/node": "20.17.0",
    "dotenv": "17.4.1",
    "postgres": "3.4.9",
    "typescript": "5.9.3",
    "vitest": "3.2.6"
  },
  "dependencies": {
    "envio": "3.6.1",
    "viem": "2.47.11"
  },
  "engines": {
    "node": ">=22.0.0"
  }
}
